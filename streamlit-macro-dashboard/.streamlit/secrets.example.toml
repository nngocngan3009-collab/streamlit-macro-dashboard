# This is an EXAMPLE. Do NOT commit real secrets to GitHub.
# In Streamlit Community Cloud, set these in Settings → Secrets instead.
# Then access in code via: st.secrets["GEMINI_API_KEY"]

GEMINI_API_KEY = "your_api_key_here"